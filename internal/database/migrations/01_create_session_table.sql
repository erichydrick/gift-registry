CREATE TABLE IF NOT EXISTS session (session_id UUID PRIMARY KEY, person_id SERIAL REFERENCES person(person_id), expiration TIMESTAMP NOT NULL, ip INET NOT NULL, user_agent BPCHAR, created_on TIMESTAMP NOT NULL, modified_on TIMESTAMP NOT NULL);
