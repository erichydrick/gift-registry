CREATE DATABASE gift_registry WITH ENCODING='UTF8' LC_CTYPE='en_US.utf8' LC_COLLATE='en_US.utf8';
COMMIT;

CREATE TABLE IF NOT EXISTS migrations (id BIGINT PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY, filename VARCHAR(255), appliedOn TIMESTAMP);
COMMIT;
